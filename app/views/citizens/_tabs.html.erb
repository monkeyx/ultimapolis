<ul class="nav nav-tabs">
  <li class="<%=(current_tab == 'profile' ? 'active' : '')%>"><a href="#profile" data-toggle="tab" aria-expanded="true">Profile</a></li>
  <li class="<%=(current_tab == 'inventory' ? 'active' : '')%>"><a href="#inventory" data-toggle="tab" aria-expanded="true">Inventory</a></li>
  <li class="<%=(current_tab == 'facilities' ? 'active' : '')%>"><a href="#facilities" data-toggle="tab" aria-expanded="true">Facilities</a></li>
  <li class="<%=(current_tab == 'projects' ? 'active' : '')%>"><a href="#projects" data-toggle="tab" aria-expanded="true">Projects</a></li>
  <% if can?(:edit, @citizen)%>
  <li class="dropdown">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#">
      Actions <span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
      <li><%= link_to 'Build Facility', new_facility_path %></li>
      <li><%= link_to 'Change Profession', edit_citizen_path(@citizen)%></li>
    </ul>
  </li>
  <% end %>
</ul>
<div id="citizenContent" class="tab-content">
  <div class="tab-pane fade <%=(current_tab == 'profile' ? 'active in' : '')%>" id="profile">
    <%=render partial: '/citizens/profile' %>
  </div>
  <div class="tab-pane fade <%=(current_tab == 'inventory' ? 'active in' : '')%>" id="inventory">
    <%=render partial: '/citizens/inventory' %>
  </div>
  <div class="tab-pane fade <%=(current_tab == 'facilities' ? 'active in' : '')%>" id="facilities">
    <%=render partial: '/facilities/index' %>
  </div>
  <div class="tab-pane fade <%=(current_tab == 'projects' ? 'active in' : '')%>" id="projects">
    <%=render partial: '/projects/index' %>
  </div>
</div>