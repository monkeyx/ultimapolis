<% content_for :title, "#{@profession.name} [#{@profession.profession_group}]" %>
<% content_for :lead, "<p>#{@profession.description}</p>".html_safe %>

<h2>Skills</h2>
<ul class="list-group">
<% @profession.skills.each do |skill| %>
	<li class="list-group-item"><%=link_to skill.name, skill %></li>
<% end %>
</ul>